services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5090:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - Frontiers__BaseUrl=https://organizations-api.frontiersin.org
